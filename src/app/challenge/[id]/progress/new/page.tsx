import ProgressNewLayout from "../../../../../components/progress-new/ProgressNewLayout";

type Props = {
  params: {
    id: string;
  };
};

export default function ProgressNewPage({ params }: Props) {
  return <ProgressNewLayout />;
} 